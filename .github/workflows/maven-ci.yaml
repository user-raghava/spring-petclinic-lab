name: Maven CI - Spring PetClinic

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:        # Allow manual trigger from GitHub UI

permissions:
  contents: read
  checks: write

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout source
        uses: actions/checkout@v4

      # 2. Setup Java
      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # 3. Build & run tests
      - name: Build with Maven
        run: mvn clean verify

      # 4. Publish JUnit test results to GitHub Checks
      - name: Publish Test Results
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Maven Unit Tests
          path: target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true

      # 5. Upload raw test report files
      - name: Upload JUnit XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-test-results
          path: target/surefire-reports/

      # 6. Upload built JAR
      - name: Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: spring-petclinic-jar
          path: target/*.jar

    #   # 7. Telegram notification for test result
    #   - name: Notify Telegram - Test Status
    #     if: always()
    #     run: |
    #       STATUS="${{ job.status }}"
    #       MESSAGE="🧪 Spring PetClinic CI\nTest Status: ${STATUS}\nRepo: ${{ github.repository }}\nRun: ${{ github.run_id }}"
    #       curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
    #         -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
    #         -d text="$MESSAGE"

    #   # 8. Telegram notification for artifact upload
    #   - name: Notify Telegram - Artifact Uploaded
    #     if: success()
    #     run: |
    #       MESSAGE="📦 Artifact uploaded successfully\nRepo: ${{ github.repository }}"
    #       curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
    #         -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
    #         -d text="$MESSAGE"
